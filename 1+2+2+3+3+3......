#include <stdio.h>
int main()
{
	int n;
	scanf("%d",&n);
	int i,j=1,k=1,sum=0;
	for( i=1; i<=n; i++ ){
		sum=sum+j;
		if( k==j ){
			j++;
			k=1;
		}else{
			k++;
		}
	}
	printf("%d\n",sum);
	return 0;
}
