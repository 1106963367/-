#include<stdio.h>
int main()                   //输入一组不定长数据存到数组中，并输出。
{
	int a[101],n=0;
	while(1)
	{
		scanf("%d",&a[n++]);
		if(getchar()=='\n')
			break;
	}
	for(int i=0;i<n;i++)
		printf("%d ",a[i]);
	return 0;
} 
